<br>
<div class="container">
    <div class="panel panel-danger">
        
      <div class="panel-heading">
        <h3> 카드 혜택 한 눈에 보기 </h3>
      </div>
      
      <div class="panel-body">
          
          <div class="well", style="background:#FFFFCC">
              <div class="row">
                  <div class="col-md-6">
                    <% @cards.where(card_id: @bankid, card_name: @cardid).find_each do |card| %>
                      <p> <%= image_tag card.avatar.url, :size => '260x180' %> </p>
                    <% end %>
                  </div>
                  
                  <div class="col-md-6">
                    <br><br><br><br>
                      <h1> 카드 이름: <%= @cardname %> </h1>
                      <h3> 발급 은행: <%= @bankname %> </h3>
                  </div>
              </div>
          </div>
          
        <div class="well">
            <h2> BAR CHART </h2><br>
               <% @cards.where(card_id: @bankid, card_name: @cardid).find_each do |card| %>
                  <%= link_to results_by_barchart, card_path(card), :target => "_blank", class: "list-group-item2" %>
               <% end %>
            <br><br>
            
            <h2> PIE CHART </h2><br>
            <% @cards.where(card_id: @bankid, card_name: @cardid).find_each do |card| %>
              <%= link_to results_by_piechart, card_path(card), :target => "_blank", class: "list-group-item2" %>
            <% end %>
        </div>
        
      </div>
    </div>
</div>



